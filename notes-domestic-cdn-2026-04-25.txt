Domestic / hybrid CDN: added Xray inbounds cdn-ir-tls-ws-2443 (VLESS+WS+TLS) and
cdn-ir-tls-xhttp-10443 (VLESS+XHTTP+TLS with xPadding obfuscation) using the existing
Let’s Encrypt certificate for cdn.dreammaker-groupsoft.ir. Point a domestic CDN origin
(ArvanCloud, Derak, etc.) at the same VPS IP:ports 2443 and 10443 with “full SSL”
terminate-at-edge or origin as supported; client SNI and Host must match
cdn.dreammaker-groupsoft.ir (or the name on the certificate). SNI in links cannot be
aparat.com for these TLS+WS inbounds: that would not match the server cert.

Backups on server before deploy: /usr/local/etc/xray/config.json.backup-* and
/etc/nginx/sites-available.backup-* (20260425-080201).

Do not store production secrets in git; one-line client URLs are generated on demand.
